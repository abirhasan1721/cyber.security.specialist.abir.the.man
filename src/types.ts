export interface Service {
  id: string;
  title: string;
  description: string;
  icon: any;
}

export interface Platform {
  name: string;
  icon: any;
  color: string;
}

export interface WhyChoice {
  title: string;
  description: string;
}
